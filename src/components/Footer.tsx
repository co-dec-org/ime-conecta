export default function Footer() {
  return (
    <footer className="site-footer">
      <p>
        Sitio estatico sin analytics, cookies ni backend. Las preferencias visuales
        se guardan solo en localStorage.
      </p>
      <p>
        La capa Web-Video + Web-Audio API se procesa localmente en el navegador.
      </p>
    </footer>
  );
}
