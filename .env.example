# Variables de entorno — copiar a .env.local y completar con los valores reales.
# .env.local está en .gitignore (NO se versiona ni se sube).
#
# Capa privada (Supabase). Vite expone solo variables con prefijo VITE_.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
