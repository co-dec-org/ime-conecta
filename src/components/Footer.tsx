export default function Footer() {
  return (
    <footer className="site-footer">
      <p>
        Analitica de visitas anonima y sin cookies (Vercel Web Analytics). Sin
        backend de datos personales: las preferencias visuales y las notas se
        guardan solo en localStorage del navegador.
      </p>
      <p>
        La capa Web-Video + Web-Audio API se procesa localmente en el navegador.
      </p>
    </footer>
  );
}
